{
  "name": "@hybrid/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Control-plane API (Hono). Step 1: liveness endpoint and composition skeleton only.",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=../../.env src/index.ts",
    "start": "node --env-file-if-exists=../../.env dist/index.js"
  },
  "dependencies": {
    "@hono/node-server": "2.1.0",
    "@hybrid/config": "workspace:*",
    "@hybrid/contracts": "workspace:*",
    "@hybrid/db": "workspace:*",
    "hono": "4.13.1"
  },
  "devDependencies": {
    "@types/node": "20.19.43",
    "tsx": "4.23.12"
  }
}
