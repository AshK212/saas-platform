{
  "name": "@hybrid/simulator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Reference client for the control-plane public API. Drives the Credit acceptance flows over HTTP with a workspace API key; holds no database access and no operator authority.",
  "main": "./dist/index.js",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@hybrid/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "20.19.43",
    "tsx": "4.23.12"
  }
}
